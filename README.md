# PesaGraph

Multi-Rail Payment Reconciliation & Liquidity Intelligence Platform for Kenya.

**This repository contains structure only.**  
All product vision, architecture decisions, bounded contexts, messaging strategy (RabbitMQ + Kafka), Docker layout, and WhatsApp conversational principles are defined in:

**[Instructions.PesaGraph.READMe.md](Instructions.PesaGraph.READMe.md)**

Open `PesaGraph.sln` (or `PesaGraph.slnx`) in Visual Studio to see the full hierarchical backend.

Frontend lives under `/frontend` (Angular skeleton).
